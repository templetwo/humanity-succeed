Read the attached humanity-succeed-spec packet and implement `humanity-succeed` in the local workspace I assign.

Treat BUILD_SPEC.md and docs/PROTOCOL.md as the build contract, with AGENTS.md defining builder boundaries. Start with WP0–WP2: contracts, observation-isolating compiler, and one complete scripted correction-versus-claim demonstration with verifiable receipts. Continue the remaining offline work packages when that slice passes.

Preserve the six-cell comparison: unchanged / procedural-control / conduct-curriculum weights, each without and with the same principles prompt. Preserve affirmative help, learning and user-valued features in the evaluator—not only refusal and honesty traps.

The code must distinguish proposal, permission, execution and actual effect. It must not reveal evaluator-only state or future events to the subject. The compiler must keep roots and derivatives in one split, and must not train rejected responses as SFT targets. Human reviews and approvals may not be invented.

Implement optional local MLX inference and SFT interfaces with mocks and explicit run gates, but do not download a model, make a real subject-model call, train weights, push a repo, publish, deploy, change the Stack, or modify another repository under this build assignment. Do not claim that a scripted test is a model result.

Return the local commit, a working offline demonstration, exact test receipts, acceptance matrix, preregistration draft, actual workload arithmetic, and a handoff showing what is built, what is unverified and which next action needs my approval. Stop before real model execution or training.
