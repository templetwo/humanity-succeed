# Source register and verification status

Checked for this specification on 2026-09-24. Implementation requirements in BUILD_SPEC and PROTOCOL are proposed design choices unless a source is explicitly cited. No research result below is a result from this project.

## S1 — Anthony's request and this conversation

Primary project intent: adapt a small model toward human goodness, beauty and humanity's success; preserve the distinction between meaningful assistance and performative approval. The latest instruction requests a repository specification for a terminal agent. The six-cell design, instrument-first sequence and action/effect distinction were developed in the preceding relay. This specification does not expand that request into permission to run training or publish.

## S2 — Supplied relay documents

The three attachment identities and exact byte hashes are recorded in INPUT_RECEIPTS.json. The latest Round-4 document was retrieved in full through the file reader; the earlier documents were already present in the conversation. These are contributed proposals and research assessments, not independent measurements by this specification author. Their full text is not copied into training data or redistributed in this packet.

Round-4 locations most relevant to implementation: four amendments in its reply; matched training and prompt leakage; scenario-level clustering; optional eight-demonstration cell; commissioning table; pilot/preregistration proposal. DECISIONS.md records where the spec adopts or corrects them. Its entire wider literature survey was not independently re-verified here.

## S3 — project-epistemic-bound and temple-harness

Repository references:

```text
https://github.com/templetwo/project-epistemic-bound
https://github.com/templetwo/temple-harness
```

The prior turn retrieved the README documents and recorded their proposal/permission/effect distinction and unruled relational-archive boundary. Current attempts to open PEB's SCENARIO_FAMILIES.md, ARCHITECTURE.md and INTERFACES.md, through both GitHub and raw URLs, failed. Their contents, actual current contracts, complete six-family list, licensing and release acceptance were not verified in this specification session. A local compatibility audit is WP0 work. No compatibility is claimed by this packet.

## S4 — MLX LM official training documentation

```text
https://github.com/ml-explore/mlx-lm/blob/main/mlx_lm/LORA.md
```

Retrieved directly in this session. Supports local SFT/LoRA, quantized adaptation, separate adapter generation, JSONL data and explicit prompt masking. The documented chat mask trains the final message as completion, motivating the prefix-per-assistant-turn compiler requirement. It does not establish that the user's exact hardware/model combination fits or that any run was executed. The builder must pin and test its actual version and command surface.

## S5 — Paired equivalence reference

```text
https://www.statsmodels.org/stable/generated/statsmodels.stats.weightstats.ttost_paired.html
```

Official paired-TOST API reference retrieved. It illustrates the equivalence test's explicit lower/upper bounds. It is not, by itself, the validated estimator for this project's correlated episodes. PROTOCOL's clustered inference, multiplicity and qualification are design requirements, not claims that a standard paired-t function automatically handles them. A direct request for the Lakens 2017 PMC article returned a browser challenge, so this session did not newly verify its text through that page.

## S6 — Subliminal learning

```text
https://arxiv.org/abs/2507.14805
https://www.nature.com/articles/s41586-026-10319-8
```

Previously discussed primary research and cited in the supplied Round-4 correction. This session did not re-fetch the full paper. The drafting precaution is bounded: record known/unknown initialization relationships; do not infer safety from brand/family difference alone. No automatic corpus-cleanliness claim or model-family universal is made.

## S7 — Limited positive-transfer precedent and scale caution

```text
https://arxiv.org/html/2606.09475v2
https://arxiv.org/abs/2609.20412
```

The June paper's HTML was read directly: Del Pinal et al., *Emergent alignment and the projectability of ethical personas*. It reports constitution-guided narrow safety training and broader/held-out safety evaluation from a helpful-only Mistral-7B source. That is a limited safety-response precedent, not sustained good conduct or an AGI claim.

The September abstract was retrieved: Baines et al., *Stress-testing Alignment Midtraining*, submitted 2026-09-17. It concerns alignment midtraining and limitations of generalization. The present project studies small-model SFT/LoRA; findings from a different procedure do not establish a universal parameter cutoff or pre-decide this experiment. The Round-4 detailed scale figures were not independently rechecked here.

## S8 — Evaluation-cue sensitivity and trace limits

```text
https://arxiv.org/html/2506.18032v1
https://www.anthropic.com/research/reasoning-models-dont-say-think
```

Primary references considered in the prior relay, not newly re-fetched in this turn. The build retains the bounded interpretations: a cue-dependent change is not proof of a particular motive, and a reasoning trace is not an internal-recognition certificate. The protocol uses actual observations/actions/effects instead.

## Operational receipt

Stack witness_boot, where_did_i_leave_off and my_toolkit were called. witness_boot identifies itself as an unimplemented Phase-6 placeholder with active transport constraints; it is not a passed runtime identity test. compass_check returned a required-action schema error and no clearance. There was no persistent Stack proposal/write, public publication, external repository mutation, model call, or training run in this specification task.
