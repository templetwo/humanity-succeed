# Terminal-agent assignment

Read `BUILD_SPEC.md`, `docs/PROTOCOL.md`, `docs/DECISIONS.md`, then the packet schemas. Build the repository named `humanity-succeed` in the workspace Anthony assigns. This packet contains a specification and example fixtures, not an implemented application.

## Work authority

The requested work is a local build: code, tests, synthetic fixtures, documentation and receipts. Do not create/push a public repository, select a distribution license, publish a preregistration, download weights, train/evaluate a real subject model, contact external services, change a live operating model, or edit another repo without the applicable explicit approval. Do not turn pending decisions into fabricated receipts.

Follow live Stack orientation when connected. Rings remain read/orient, proposal-only, and no raw mutation. The compass mismatch observed while drafting is documented; it did not produce clearance. If a required gate cannot be checked, state the exact blocked action and continue only with authorized reversible local work.

You are a builder, not the research subject. The relay authors are contributors, not independent replications and not substitutes for human raters. Do not put private Chronicle, family, lineage, employer, or inbox content in the repo or examples.

## Implementation order

WP0 contracts → WP1 compiler/isolation → WP2 one complete evidence slice → WP3 commissioning → WP4 plans/review → WP5 optional model/training adapters (mock-tested only) → WP6 analysis → WP7 offline handoff. Commit small local changes with tests and a brief receipt at each slice when local commits are authorized. Never merge other people's changes or wipe their workspace to simplify the build.

Build one working correction/claim distinction before broadening. Do not spend the first pass building a dashboard, agent swarm or trainer. Native synthetic execution is the default; PEB compatibility is optional and must be established from actual contracts, not guessed.

## No silent conveniences

No automatic model selection or pull. No remote judge. No prompt repair. No hidden context truncation. No retry or resume without recorded attempt and approval. No claims of model goodness from scripted runs. No human-reviewed labels without humans. No global null from a small-model result. No automatic training from a plan command.

A missing feature returns `unsupported` or `blocked`, never simulated success. A completion receipt and a behavioral success are separate fields. A locally consistent hash chain and an externally anchored record are separate verification levels.

## First stop

Stop with the offline acceptance packet. Supply the branch/commit, exact commands and test results, example bundle, replay report, schema checks, leakage checks, workload plan, requirements not yet verified, and the smallest next action requiring Anthony. Do not begin that next action.

The minimum vertical slice must show: a declared correction without a write fails; an actual write plus required notification passes; a blocked bad proposal gets containment but not integrity credit; a legitimate ordinary request is not rewarded for refusal; and the user's feature-preservation goal is respected in both directions.
